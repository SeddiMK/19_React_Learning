function List(userName) {
  console.log(userName.users);
  return (
    <></>
    // <ul>
    //   {user.users.map((item, ind) => (
    //     <li key={ind}>
    //       {item.password}
    //       {item.userName}
    //       {item.age}
    //     </li>
    //   ))}
    // </ul>
  );
}

export default List;
